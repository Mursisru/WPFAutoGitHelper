<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <Color x:Key="ColorAccent">#3B82F6</Color>
    <Color x:Key="ColorAccentHover">#60A5FA</Color>
    <Color x:Key="ColorBg">#000000</Color>
    <Color x:Key="ColorSurface">#0C0C0C</Color>
    <Color x:Key="ColorBorder">#333333</Color>
    <Color x:Key="ColorText">#F5F5F5</Color>
    <Color x:Key="ColorMuted">#9CA3AF</Color>
    <Color x:Key="ColorHeader">#FFFFFF</Color>
    <Color x:Key="ColorTabInactive">#0A0A0A</Color>
    <Color x:Key="ColorTabHover">#1F1F1F</Color>
    <Color x:Key="ColorInputBg">#000000</Color>
    <Color x:Key="ColorInputFg">#F5F5F5</Color>
    <Color x:Key="ColorSecondaryBtn">#1A1A1A</Color>
    <Color x:Key="ColorSecondaryBtnHover">#2A2A2A</Color>
    <Color x:Key="ColorHelpPanelBg">#0C0C0C</Color>
    <Color x:Key="ColorHelpPanelBorder">#333333</Color>

    <SolidColorBrush x:Key="BrushAccent" Color="{StaticResource ColorAccent}"/>
    <SolidColorBrush x:Key="BrushAccentHover" Color="{StaticResource ColorAccentHover}"/>
    <SolidColorBrush x:Key="BrushBg" Color="{StaticResource ColorBg}"/>
    <SolidColorBrush x:Key="BrushSurface" Color="{StaticResource ColorSurface}"/>
    <SolidColorBrush x:Key="BrushBorder" Color="{StaticResource ColorBorder}"/>
    <SolidColorBrush x:Key="BrushText" Color="{StaticResource ColorText}"/>
    <SolidColorBrush x:Key="BrushMuted" Color="{StaticResource ColorMuted}"/>
    <SolidColorBrush x:Key="BrushHeader" Color="{StaticResource ColorHeader}"/>
    <SolidColorBrush x:Key="BrushTabInactive" Color="{StaticResource ColorTabInactive}"/>
    <SolidColorBrush x:Key="BrushTabHover" Color="{StaticResource ColorTabHover}"/>
    <SolidColorBrush x:Key="BrushInputBg" Color="{StaticResource ColorInputBg}"/>
    <SolidColorBrush x:Key="BrushInputFg" Color="{StaticResource ColorInputFg}"/>
    <SolidColorBrush x:Key="BrushSecondaryBtn" Color="{StaticResource ColorSecondaryBtn}"/>
    <SolidColorBrush x:Key="BrushSecondaryBtnHover" Color="{StaticResource ColorSecondaryBtnHover}"/>
    <SolidColorBrush x:Key="BrushLogBg" Color="#000000"/>
    <SolidColorBrush x:Key="BrushLogFg" Color="#E5E5E5"/>
    <SolidColorBrush x:Key="BrushHelpPanelBg" Color="{StaticResource ColorHelpPanelBg}"/>
    <SolidColorBrush x:Key="BrushHelpPanelBorder" Color="{StaticResource ColorHelpPanelBorder}"/>

    <Style x:Key="AppWindowStyle" TargetType="Window">
        <Setter Property="Background" Value="{DynamicResource BrushBg}"/>
        <Setter Property="Foreground" Value="{DynamicResource BrushText}"/>
        <Setter Property="FontFamily" Value="Segoe UI"/>
        <Setter Property="FontSize" Value="13"/>
    </Style>

    <Style TargetType="CheckBox">
        <Setter Property="Foreground" Value="{DynamicResource BrushText}"/>
        <Setter Property="Margin" Value="0,2"/>
    </Style>

    <Style TargetType="RadioButton">
        <Setter Property="Foreground" Value="{DynamicResource BrushText}"/>
        <Setter Property="Margin" Value="0,2"/>
    </Style>

    <Style TargetType="Expander">
        <Setter Property="Foreground" Value="{DynamicResource BrushText}"/>
    </Style>

    <Style x:Key="AppDataGridStyle" TargetType="DataGrid">
        <Setter Property="Background" Value="{DynamicResource BrushSurface}"/>
        <Setter Property="Foreground" Value="{DynamicResource BrushText}"/>
        <Setter Property="BorderBrush" Value="{DynamicResource BrushBorder}"/>
        <Setter Property="RowBackground" Value="{DynamicResource BrushSurface}"/>
        <Setter Property="AlternatingRowBackground" Value="{DynamicResource BrushBg}"/>
        <Setter Property="GridLinesVisibility" Value="Horizontal"/>
        <Setter Property="HorizontalGridLinesBrush" Value="{DynamicResource BrushBorder}"/>
        <Setter Property="HeadersVisibility" Value="Column"/>
    </Style>

    <Style TargetType="DataGridColumnHeader">
        <Setter Property="Background" Value="{DynamicResource BrushSecondaryBtn}"/>
        <Setter Property="Foreground" Value="{DynamicResource BrushHeader}"/>
        <Setter Property="BorderBrush" Value="{DynamicResource BrushBorder}"/>
        <Setter Property="BorderThickness" Value="0,0,0,1"/>
        <Setter Property="Padding" Value="8,6"/>
        <Setter Property="FontWeight" Value="SemiBold"/>
    </Style>

    <Style TargetType="DataGridCell">
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="Foreground" Value="{DynamicResource BrushText}"/>
        <Setter Property="BorderBrush" Value="{DynamicResource BrushBorder}"/>
        <Setter Property="Padding" Value="6,4"/>
    </Style>

    <Style x:Key="MainTabItemStyle" TargetType="TabItem">
        <Setter Property="Foreground" Value="{DynamicResource BrushMuted}"/>
        <Setter Property="FontSize" Value="13"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="TabItem">
                    <Border x:Name="TabBorder" Background="{DynamicResource BrushTabInactive}"
                            BorderBrush="{DynamicResource BrushBorder}" BorderThickness="1"
                            CornerRadius="6,6,0,0" Padding="14,8" Margin="0,0,4,0">
                        <ContentPresenter ContentSource="Header"
                                          TextElement.Foreground="{TemplateBinding Foreground}"/>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsSelected" Value="True">
                            <Setter TargetName="TabBorder" Property="Background" Value="{DynamicResource BrushSurface}"/>
                            <Setter Property="Foreground" Value="{DynamicResource BrushHeader}"/>
                            <Setter Property="FontWeight" Value="SemiBold"/>
                        </Trigger>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="TabBorder" Property="Background" Value="{DynamicResource BrushTabHover}"/>
                            <Setter Property="Foreground" Value="{DynamicResource BrushText}"/>
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter Property="Opacity" Value="0.5"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="MainTabControlStyle" TargetType="TabControl">
        <Setter Property="Background" Value="{DynamicResource BrushBg}"/>
        <Setter Property="BorderThickness" Value="0"/>
        <Setter Property="Padding" Value="0"/>
        <Setter Property="ItemContainerStyle" Value="{StaticResource MainTabItemStyle}"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="TabControl">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>
                        <TabPanel Grid.Row="0" IsItemsHost="True" Background="Transparent" Margin="0,0,0,0"/>
                        <Border Grid.Row="1" Background="{DynamicResource BrushSurface}"
                                BorderBrush="{DynamicResource BrushBorder}" BorderThickness="1"
                                CornerRadius="0,8,8,8" Padding="4">
                            <ContentPresenter ContentSource="SelectedContent"/>
                        </Border>
                    </Grid>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="HeaderTitleStyle" TargetType="TextBlock">
        <Setter Property="FontSize" Value="20"/>
        <Setter Property="FontWeight" Value="SemiBold"/>
        <Setter Property="Foreground" Value="{DynamicResource BrushHeader}"/>
    </Style>

    <Style x:Key="HeaderSubtitleStyle" TargetType="TextBlock">
        <Setter Property="FontSize" Value="12"/>
        <Setter Property="Foreground" Value="{DynamicResource BrushMuted}"/>
        <Setter Property="Margin" Value="0,2,0,0"/>
    </Style>

    <Style x:Key="StepBadgeStyle" TargetType="Border">
        <Setter Property="Background" Value="{DynamicResource BrushAccent}"/>
        <Setter Property="CornerRadius" Value="10"/>
        <Setter Property="Width" Value="22"/>
        <Setter Property="Height" Value="22"/>
        <Setter Property="Margin" Value="0,0,8,0"/>
    </Style>

    <Style x:Key="CardGroupStyle" TargetType="GroupBox">
        <Setter Property="Margin" Value="0,0,0,12"/>
        <Setter Property="Padding" Value="10"/>
        <Setter Property="BorderBrush" Value="{DynamicResource BrushBorder}"/>
        <Setter Property="Background" Value="{DynamicResource BrushBg}"/>
        <Setter Property="Foreground" Value="{DynamicResource BrushText}"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="GroupBox">
                    <Border Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="1"
                            CornerRadius="8"
                            Padding="2">
                        <DockPanel>
                            <TextBlock DockPanel.Dock="Top"
                                       Text="{TemplateBinding Header}"
                                       FontWeight="SemiBold"
                                       Margin="8,6,8,4"
                                       Foreground="{DynamicResource BrushHeader}"/>
                            <ContentPresenter Margin="6,0,6,6"/>
                        </DockPanel>
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="PrimaryButtonStyle" TargetType="Button">
        <Setter Property="MinHeight" Value="30"/>
        <Setter Property="Padding" Value="12,4"/>
        <Setter Property="Margin" Value="0,0,8,8"/>
        <Setter Property="Background" Value="{DynamicResource BrushAccent}"/>
        <Setter Property="Foreground" Value="White"/>
        <Setter Property="BorderThickness" Value="0"/>
        <Setter Property="Cursor" Value="Hand"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Border x:Name="Bd" Background="{TemplateBinding Background}"
                            CornerRadius="6" Padding="{TemplateBinding Padding}">
                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"
                                          TextElement.Foreground="{TemplateBinding Foreground}"/>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="Bd" Property="Background" Value="{DynamicResource BrushAccentHover}"/>
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter TargetName="Bd" Property="Opacity" Value="0.4"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="SecondaryButtonStyle" TargetType="Button">
        <Setter Property="MinHeight" Value="30"/>
        <Setter Property="Padding" Value="12,4"/>
        <Setter Property="Margin" Value="0,0,8,8"/>
        <Setter Property="Background" Value="{DynamicResource BrushSecondaryBtn}"/>
        <Setter Property="Foreground" Value="{DynamicResource BrushText}"/>
        <Setter Property="BorderBrush" Value="{DynamicResource BrushBorder}"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="Cursor" Value="Hand"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Border x:Name="Bd" Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            CornerRadius="6" Padding="{TemplateBinding Padding}">
                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"
                                          TextElement.Foreground="{TemplateBinding Foreground}"/>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="Bd" Property="Background" Value="{DynamicResource BrushSecondaryBtnHover}"/>
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter TargetName="Bd" Property="Opacity" Value="0.4"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="HelpPanelStyle" TargetType="Border">
        <Setter Property="Background" Value="{DynamicResource BrushHelpPanelBg}"/>
        <Setter Property="BorderBrush" Value="{DynamicResource BrushHelpPanelBorder}"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="CornerRadius" Value="6"/>
        <Setter Property="Padding" Value="10"/>
        <Setter Property="Margin" Value="0,0,0,8"/>
    </Style>

    <Style x:Key="SettingsPanelStyle" TargetType="Border">
        <Setter Property="Background" Value="{DynamicResource BrushSurface}"/>
        <Setter Property="BorderBrush" Value="{DynamicResource BrushBorder}"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="CornerRadius" Value="8"/>
        <Setter Property="Padding" Value="12"/>
        <Setter Property="Margin" Value="0,0,0,10"/>
    </Style>

    <Style x:Key="LogTextBoxStyle" TargetType="TextBox">
        <Setter Property="Background" Value="{DynamicResource BrushLogBg}"/>
        <Setter Property="Foreground" Value="{DynamicResource BrushLogFg}"/>
        <Setter Property="BorderThickness" Value="0"/>
        <Setter Property="FontFamily" Value="Consolas"/>
        <Setter Property="FontSize" Value="12"/>
    </Style>

    <ResourceDictionary.MergedDictionaries>
        <ResourceDictionary Source="InputControls.xaml"/>
    </ResourceDictionary.MergedDictionaries>

</ResourceDictionary>
